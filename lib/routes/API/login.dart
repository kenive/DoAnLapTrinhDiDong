   

   
/* Future<List<Account>> fethlogin(a,b) async {
 
  final response = await http.get(Uri.parse('http://10.0.2.2:8000/api/account/$a/$b'));

  var list = jsonDecode(response.body);

  return list.map<Account>((model) => Account.fromJson(model)).toList();


} */

 
  /* var list = jsonDecode(response.body);
     if (response.statusCode == 200) {
    
    /* Navigator.pushNamed(context, '/second'); */
  */
   /*  else{
  
  ScaffoldMessenger.of(context).showSnackBar( const SnackBar(content: Text('Đăng Nhập Thất Bại'),));
    }
 */